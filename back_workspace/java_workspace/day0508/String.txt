모든 프로그래밍 언어에서 개발자가 사용할 수 있는 기본 자료형은 "문자, 숫자, 논리값"

우리가 일상에서 사용하는 문자열을 사용하렬면 문자 배열을 이용해야한다.
개발자들을 위해서 문자열에 대해서는 개체자료형을 지원해주지만 내부적으로는 배열로 처리된다.

new를 명시하지 않아도 내부적으로 객체를 생성시키는 법을 암시적(implicit) 생성법이라한다.
new 연산자를 이용하여 객체를 생성시키는 법을 명시적(explicit) 생성법이라한다.